## start=0.0 end=2.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=StoichiometryMath,Compartment,Species,Reaction,Parameter,AssignmentRule,Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00609()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C;

  // Assignment Rules:
  S3 := k1*S2;
  S1_S2_stoichiometry := 4*p1;

  // Reactions:
  reaction1: S1 => S1_S2_stoichiometry S2; C*k2*S1;

  // Species initializations:
  S1 = 0.00015/C;
  S1 has substance_per_volume;
  S2 = 0.00015/C;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 5;
  p1 = 0.5;

  // Other declarations:
  const C, k1, k2, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;
end

case00609 is "case00609"
