## start=0.0 end=6.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,C amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,RateRule,Amount,NonConstantCompartment,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00051()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Rate Rules:
  C' = -1*C*p1;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 1.5/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = 0.1;
  k1 = 0.9;

  // Other declarations:
  var C;
  const p1, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00051 is "case00051"
