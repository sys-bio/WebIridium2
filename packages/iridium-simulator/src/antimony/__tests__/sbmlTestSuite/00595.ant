## start=0.0 end=3.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Compartment,Species,Reaction,Concentration,LocalParameters

// Created by libAntimony v3.0.0
model *case00595()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 => S2; C*reaction1_k*S1;
  reaction2: S2 => S3; C*reaction2_k*S2;

  // Species initializations:
  S1 = 0.003;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  reaction1_k = 1;
  reaction2_k = 2;

  // Other declarations:
  const C;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00595 is "case00595"
