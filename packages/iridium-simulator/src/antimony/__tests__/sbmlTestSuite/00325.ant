## start=0.0 end=8.0 points=50 atol=0.01 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Compartment,Species,Reaction,Parameter,RateRule,Amount

// Created by libAntimony v3.0.0
model *case00325()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, $S3 in compartment_, $S4 in compartment_;

  // Rate Rules:
  S3' = 0.5*k1;
  S4' = -0.5*k2;

  // Reactions:
  reaction1: S1 => S2; compartment_*k1*S1;

  // Species initializations:
  S1 = 1.5/compartment_;
  S1 has substance_per_area;
  S2 = 2/compartment_;
  S2 has substance_per_area;
  S3 = 1.5/compartment_;
  S3 has substance_per_area;
  S4 = 4/compartment_;
  S4 has substance_per_area;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has area;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case00325 is "case00325"
