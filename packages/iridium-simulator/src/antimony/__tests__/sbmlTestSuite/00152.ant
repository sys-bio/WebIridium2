## start=0.0 end=0.1 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00152()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, $S3 in compartment_;

  // Assignment Rules:
  S3 := k1*S2;

  // Reactions:
  reaction1: S1 => S2; compartment_*k2*S1;

  // Species initializations:
  S1 = 1/compartment_;
  S1 has substance_per_area;
  S2 = 1.5e-15/compartment_;
  S2 has substance_per_area;
  S3 has substance_per_area;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has area;

  // Variable initializations:
  k1 = 0.75;
  k2 = 50;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case00152 is "case00152"
