## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=CSymbolRateOf,CSymbolTime,EventWithDelay,Parameter,RateRule,EventIsPersistent,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01525()

  // Rate Rules:
  P1' = time;

  // Events:
  E0: at 2 after (rateOf(P1) > 1.5) && (rateOf(P1) < 2.5): P2 = 3;

  // Variable initializations:
  P1 = 0;
  P2 = 0;

  // Other declarations:
  var P1, P2;
end

case01525 is "case01525"
