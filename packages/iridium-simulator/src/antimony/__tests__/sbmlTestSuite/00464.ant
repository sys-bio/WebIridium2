## start=0.0 end=5.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2 amounts=
## tags=Compartment,Species,Reaction,Parameter,Concentration

// Created by libAntimony v3.0.0
model *case00464()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;
  reaction2: S2 => S1; C*k2*S2;

  // Species initializations:
  S1 = 0.0015;
  S1 has substance_per_volume;
  S2 = 0.0015;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.5;
  k2 = 0.5;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00464 is "case00464"
