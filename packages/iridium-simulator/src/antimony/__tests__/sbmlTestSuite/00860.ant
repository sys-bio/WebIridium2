## start=0.0 end=5.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2 amounts=
## tags=Compartment,Species,Reaction,Parameter,CSymbolTime,Concentration,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00860()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1*time;

  // Species initializations:
  S1 = 0.0015;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 0.9;
  C has volume;

  // Variable initializations:
  k1 = 1;

  // Other declarations:
  const C, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00860 is "case00860"
