## start=0.0 end=10.0 points=100 atol=0.0001 rtol=0.0001 selections=X,Y,Z,Q amounts=X
## tags=AssignmentRule,Compartment,InitialAssignment,Parameter,RateRule,Reaction,Species,Amount,InitialValueReassigned,NonConstantParameter,SpeciesReferenceInMath

// Created by libAntimony v3.0.0
model *case00974()

  // Compartments and Species:
  species X;

  // Assignment Rules:
  Z := Xref;

  // Rate Rules:
  Y' = Xref;

  // Reactions:
  J0:  -> Xref X; k1;

  // Species initializations:
  X = 0;

  // Variable initializations:
  Y = 0;
  Xref = 1;
  Q = Xref;
  k1 = 1;

  // Other declarations:
  var Y, Z;
  const Q, k1;
end

case00974 is "case00974"
