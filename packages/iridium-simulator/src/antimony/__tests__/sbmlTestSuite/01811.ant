## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=INF,Inf,inf,a,b,c,d amounts=
## tags=InitialAssignment,Parameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01811()

  // Variable initializations:
  INF_ = 10;
  Inf = 1;
  inf_ = 0.1;
  a = INF_;
  b = Inf;
  c = inf_;
  d = INF;

  // Other declarations:
  const INF_, Inf, inf_, a, b, c, d;
end
