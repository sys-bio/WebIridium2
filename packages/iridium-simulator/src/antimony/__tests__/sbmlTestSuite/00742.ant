## start=0.0 end=5.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Compartment,Species,Reaction,Parameter,FunctionDefinition,RateRule,Concentration,NonConstantParameter,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00742()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Rate Rules:
  k1' = p1*(k2*p2 + k3);

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 2;
  S2 has substance_per_volume;
  S3 = 1;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1.25;
  C has volume;

  // Variable initializations:
  k1 = 1.7;
  p1 = 1;
  k2 = 0.3;
  p2 = 1;
  k3 = -0.1;

  // Other declarations:
  var k1;
  const C, p1, k2, p2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00742 is "case00742"
