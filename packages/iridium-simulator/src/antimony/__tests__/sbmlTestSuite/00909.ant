## start=0.0 end=3.0 points=50 atol=1e-05 rtol=0.0001 selections=C1,C2 amounts=
## tags=Compartment,RateRule,NonConstantCompartment,NonUnityCompartment,MultiCompartment

// Created by libAntimony v3.0.0
model *case00909()

  // Compartments and Species:
  compartment C1, C2;

  // Rate Rules:
  C1' = 0.4*C1 + 0.25*C2;
  C2' = 0.15*C1 + 0.2*C2;

  // Compartment initializations:
  C1 = 0.015;
  C1 has volume;
  C2 = 0.01;
  C2 has length;

  // Other declarations:
  var C1, C2;

  // Unit definitions:
  unit volume = litre;
  unit length = metre;
  unit time_unit = time_unit;
end

case00909 is "case00909"
