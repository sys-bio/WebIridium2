## start=0.0 end=8.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,InitialAssignment,Amount,ReversibleReaction,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01036()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S3 -> S1 + S2; -(C*(kf*S1*S2 + -1*kr*S3));

  // Species initializations:
  S1 = 2*p1;
  S1 has substance_per_volume;
  S2 = 0.5/C;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = 0.5;
  kf = 1.1;
  kr = 0.09;

  // Other declarations:
  const C, p1, kf, kr;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case01036 is "case01036"
