## start=0.0 end=10.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,CSymbolTime,Reaction,Parameter,InitialAssignment,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00878()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 0.015/C;
  S1 has substance_per_volume;
  S2 = p1*S1*time;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = -0.5;
  k1 = 1;

  // Other declarations:
  const C, p1, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00878 is "case00878"
