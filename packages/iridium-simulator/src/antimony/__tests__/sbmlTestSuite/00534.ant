## start=0.0 end=10.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Compartment,Species,Reaction,Parameter,AlgebraicRule,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00534()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Algebraic Rules:
  _alg0: 0 = k2 + -1*k3 + -0.2;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;
  reaction3: S3 => S1 + S4; C*k3*S3;

  // Species initializations:
  S1 = 0.02/C;
  S1 has substance_per_volume;
  S2 = 0.02/C;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;
  S4 = 0;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 100;
  k2 = ;
  k3 = 0.7;

  // Other declarations:
  var k2;
  const C, k1, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00534 is "case00534"
