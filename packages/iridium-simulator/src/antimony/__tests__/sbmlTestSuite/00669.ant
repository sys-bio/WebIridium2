## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3,S4,S5 amounts=S1,S2,S3,S4,S5
## tags=Compartment,Species,Reaction,Parameter,FunctionDefinition,AssignmentRule,Amount,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00669()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, $S4 in C, $S5 in C;

  // Assignment Rules:
  S4 := S3*(p1 + 1)^-1;
  S5 := S4*p1;

  // Reactions:
  reaction1: S1 => S3; C*k1*S1;
  reaction2: S3 => S2; C*k2*S5;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;
  S4 has substance_per_volume;
  S5 has substance_per_volume;

  // Compartment initializations:
  C = 2.5;
  C has volume;

  // Variable initializations:
  p1 = 2.5;
  k1 = 0.1;
  k2 = 0.15;

  // Other declarations:
  const C, p1, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00669 is "case00669"
