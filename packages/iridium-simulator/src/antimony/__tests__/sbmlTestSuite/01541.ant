## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,S2,P1,P2,C amounts=S1,S2
## tags=AssignmentRule,CSymbolRateOf,Compartment,Parameter,Reaction,Species,Amount,BoundaryCondition,InitialValueReassigned,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01541()

  // Compartments and Species:
  compartment C;
  species $S1 in C, S2 in C;

  // Assignment Rules:
  P1 := rateOf(S1);
  P2 := rateOf(S2);

  // Reactions:
  J0: $S1 -> S2; 1;

  // Species initializations:
  S1 = 2/C;
  S2 = 3/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  var P1, P2;
  const C;
end

case01541 is "case01541"
