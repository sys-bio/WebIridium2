## start=0.0 end=12.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,RateRule,Amount,NonConstantCompartment,NonUnityCompartment,NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00312()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Assignment Rules:
  C := p1*p2;

  // Rate Rules:
  p2' = 0.1;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 1.5/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  C has volume;

  // Variable initializations:
  p1 = 0.1;
  p2 = 1.5;
  k1 = 0.9;

  // Other declarations:
  var C, p2;
  const p1, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00312 is "case00312"
