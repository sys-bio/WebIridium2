## start=0.0 end=5.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,EventWithDelay,Amount

// Created by libAntimony v3.0.0
model *case00424()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Events:
  event1: at 2.25 after S1 < 0.45: S1 = 0.6, S2 = 1.25;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_length;
  S2 = 2/C;
  S2 has substance_per_length;
  S3 = 1/C;
  S3 has substance_per_length;

  // Compartment initializations:
  C = 1;
  C has length;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit length = metre;
  unit time_unit = time_unit;
  unit substance_per_length = mole / metre;
end

case00424 is "case00424"
