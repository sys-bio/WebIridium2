## start=0.0 end=8.0 points=50 atol=0.001 rtol=0.0001 selections=c amounts=
## tags=Compartment,RateRule,InitialAssignment,NonConstantCompartment,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00917()

  // Compartments and Species:
  compartment c;

  // Rate Rules:
  c' = 0.5*c;

  // Compartment initializations:
  c = 2.2/3;
  c has length;

  // Other declarations:
  var c;

  // Unit definitions:
  unit length = metre;
  unit time_unit = time_unit;
end

case00917 is "case00917"
