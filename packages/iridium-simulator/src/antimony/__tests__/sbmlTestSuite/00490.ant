## start=0.0 end=20.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,InitialAssignment,Amount,0D-Compartment

// Created by libAntimony v3.0.0
model *case00490()

  // Compartments and Species:
  compartment C;
  substanceOnly species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; k1*S1*S2;
  reaction2: S3 => S1 + S2; k2*S3;

  // Species initializations:
  S1 = 2*p1;
  S1 has substance_per_volume;
  S2 = 0.0002/C;
  S2 has substance_per_volume;
  S3 = 0.0001/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = ;

  // Variable initializations:
  p1 = 1.25e-05;
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const C, p1, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00490 is "case00490"
