## start=0.0 end=5.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=StoichiometryMath,Compartment,Species,Reaction,Parameter,Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00068()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_;

  // Assignment Rules:
  S1_S2_stoichiometry := 2*p1;

  // Reactions:
  reaction1: S1 => S1_S2_stoichiometry S2; compartment_*k1*S1;

  // Species initializations:
  S1 = 1.5/compartment_;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 1.5;
  p1 = 1;

  // Other declarations:
  const compartment_, k1, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00068 is "case00068"
