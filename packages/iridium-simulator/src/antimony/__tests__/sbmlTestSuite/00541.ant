## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=X0,X1,T amounts=X0,X1,T
## tags=Compartment,Species,Reaction,Parameter,AlgebraicRule,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00541()

  // Compartments and Species:
  compartment C;
  species X0 in C, X1 in C, T in C;

  // Algebraic Rules:
  _alg0: 0 = C + -1;

  // Reactions:
  reaction1: X0 => T; C*k1*X0;
  reaction2: T => X1; C*k2*T;

  // Species initializations:
  X0 = 1/C;
  X0 has substance_per_area;
  X1 = 0;
  X1 has substance_per_area;
  T = 0;
  T has substance_per_area;

  // Compartment initializations:
  C = ;
  C has area;

  // Variable initializations:
  k1 = 0.1;
  k2 = 0.375;

  // Other declarations:
  var C;
  const k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;
end

case00541 is "case00541"
