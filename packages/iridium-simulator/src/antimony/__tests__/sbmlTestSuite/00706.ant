## start=0.0 end=5.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,InitialAssignment,Amount,LocalParameters,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00706()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Assignment Rules:
  k := 0.75;

  // Reactions:
  reaction1: S1 => S2; C*reaction1_k*S1;

  // Species initializations:
  S1 = 0.133333333333333*k;
  S1 has substance_per_volume;
  S2 = 0.15/C;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  reaction1_k = 0.75;

  // Other declarations:
  var k;
  const C;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00706 is "case00706"
