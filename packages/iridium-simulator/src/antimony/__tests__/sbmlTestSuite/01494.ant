## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2,P3,P4,P5 amounts=
## tags=FunctionDefinition,InitialAssignment,Parameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01494()

  // Variable initializations:
  P1 = piecewise(1, 1 < 2 < 1, 3);
  P2 = piecewise(1, 2 > 1 > 2, 3);
  P3 = piecewise(1, 1 <= 2 <= 1, 3);
  P4 = piecewise(1, 2 >= 1 >= 2, 3);
  P5 = piecewise(1, 1 == 1 == 2, 3);

  // Other declarations:
  const P1, P2, P3, P4, P5;
end

case01494 is "case01494"
