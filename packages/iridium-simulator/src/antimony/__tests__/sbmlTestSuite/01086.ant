## start=0.0 end=10.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=InitialAssignment,Compartment,Species,Reaction,Parameter,AlgebraicRule,InitialValueReassigned,Amount,AssignedConstantStoichiometry,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01086()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Algebraic Rules:
  _alg0: 0 = S4 + -1*S1;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: generatedId_0 S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.1/C;
  S1 has substance_per_volume;
  S2 = 0.2/C;
  S2 has substance_per_volume;
  S3 = 0.1/C;
  S3 has substance_per_volume;
  S4 = 0.1/C;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;
  p1 = 0.5;
  generatedId_0 = 4*p1;

  // Other declarations:
  const C, k1, k2, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case01086 is "case01086"
