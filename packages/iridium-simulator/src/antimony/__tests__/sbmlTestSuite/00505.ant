## start=0.0 end=20.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Compartment,Species,Reaction,Parameter,InitialAssignment,Amount,ConstantSpecies

// Created by libAntimony v3.0.0
model *case00505()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, $S4 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S4;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.00025/C;
  S1 has substance_per_volume;
  S2 = 0.002/C;
  S2 has substance_per_volume;
  S3 = 0.001/C;
  S3 has substance_per_volume;
  S4 = 2*p1;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = 0.0005;
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const C, p1, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00505 is "case00505"
