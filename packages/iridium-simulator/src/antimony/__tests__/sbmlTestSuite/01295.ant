## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=p1,p2,C amounts=p1
## tags=CSymbolRateOf,CSymbolTime,Compartment,EventWithDelay,Parameter,Reaction,Species,Amount,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01295()

  // Compartments and Species:
  compartment C;
  species p1 in C;

  // Reactions:
  J0:  -> p1; 1;

  // Events:
  E0: at rateOf(p1) after time > 4.5: p2 = 5;

  // Species initializations:
  p1 = 1;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  p2 = 10;

  // Other declarations:
  var p2;
  const C;
end

case01295 is "case01295"
