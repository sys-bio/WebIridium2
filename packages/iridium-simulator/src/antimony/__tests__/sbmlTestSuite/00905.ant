## start=0.0 end=8.0 points=50 atol=1e-07 rtol=0.0001 selections=c amounts=
## tags=Compartment,RateRule,NonConstantCompartment,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00905()

  // Compartments and Species:
  compartment c;

  // Rate Rules:
  c' = 0.1*c;

  // Compartment initializations:
  c = 0.00015;
  c has length;

  // Other declarations:
  var c;

  // Unit definitions:
  unit length = metre;
  unit time_unit = time_unit;
end

case00905 is "case00905"
