## start=0.0 end=20.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Parameter,Reaction,Species,FunctionDefinition,Amount,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00127()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: 2 S1 + S2 => S3; compartment_*S1*S2*(k1*S1);
  reaction2: S3 => 2 S1 + S2; compartment_*k2*S3;

  // Species initializations:
  S1 = 0.001/compartment_;
  S1 has substance_per_volume;
  S2 = 0.002/compartment_;
  S2 has substance_per_volume;
  S3 = 0.001/compartment_;
  S3 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 1000;
  k2 = 0.25;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00127 is "case00127"
