## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=p1 amounts=
## tags=CSymbolAvogadro,CSymbolTime,EventWithDelay,Parameter,EventIsNotPersistent,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01661()

  // Events:
  _E0: at 1 after (time >= 0.5) && (time <= (avogadro/6.022e23)), persistent=false: p1 = 3;

  // Variable initializations:
  p1 = 1;

  // Other declarations:
  var p1;
end

case01661 is "case01661"
