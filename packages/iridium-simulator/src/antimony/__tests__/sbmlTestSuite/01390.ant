## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e,f,g amounts=
## tags=InitialAssignment,Parameter,comp:ModelDefinition,comp:Port,comp:ReplacedElement,comp:Submodel,InitialValueReassigned

// Created by libAntimony v3.0.0
model sub1(a, b, c, d, e, f, g)

  // Variable initializations:
  a = 1;
  b = a;
  c = 3;
  d = c;
  e = 4;
  f = e;
  g = 5;

  // Other declarations:
  const a, b, c, d, e, f, g;
end

sub1 is "sub1"

model *case01390()

  // Sub-modules, and any changes to those submodules:
  A: sub1(a, b, c, d, e, f, g);

  // Variable initializations:
  c = b;
  e = d;
  g = f;
end

case01390 is "case01390"
