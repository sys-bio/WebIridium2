## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=EventWithDelay,Parameter,RateRule,EventUsesAssignmentTimeValues,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01325()

  // Rate Rules:
  x' = 1;

  // Events:
  E0: at 2 after x >= 3.5, fromTrigger=false: y = x;

  // Variable initializations:
  x = 1;
  y = 0;

  // Other declarations:
  var x, y;
end

case01325 is "case01325"
