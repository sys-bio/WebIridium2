## start=0.0 end=5.0 points=125 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=
## tags=AssignmentRule,CSymbolTime,Compartment,EventWithDelay,Species,Concentration,BoundaryCondition,EventIsPersistent,EventUsesAssignmentTimeValues,InitialValueReassigned,UncommonMathML

// Created by libAntimony v3.0.0
model *case00936()

  // Compartments and Species:
  species $S1, S2;

  // Assignment Rules:
  S1 := piecewise(sin(time*10), time <= 2, 1);

  // Events:
  _E0: at 2 after S1 < 0, fromTrigger=false: S2 = S2 + 1;

  // Species initializations:
  S2 = 0;
end

case00936 is "case00936"
