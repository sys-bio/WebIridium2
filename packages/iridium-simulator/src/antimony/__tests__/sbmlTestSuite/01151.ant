## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=p8 amounts=
## tags=Parameter,RateRule,comp:Deletion,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,NonConstantParameter

// Created by libAntimony v3.0.0
model moddef1()

  // Rate Rules:
  p80' = 4*p80 + 3;

  // Variable initializations:
  p80 = 80;

  // Other declarations:
  var p80;
end

model *case01151()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.p80 is p8;

  // Rate Rules:
  p8' = 3;

  // Variable initializations:
  p8 = 8;
end

case01151 is "case01151"
