## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,c,p1 amounts=S1
## tags=Compartment,InitialAssignment,Parameter,Reaction,Species,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01224()

  // Compartments and Species:
  compartment c;
  species S1 in c;

  // Reactions:
  J0:  => S1; 1;

  // Species initializations:
  S1 = 0;

  // Compartment initializations:
  c = 1;

  // Variable initializations:
  p1 = J0;

  // Other declarations:
  var p1;
  const c;
end

case01224 is "case01224"
