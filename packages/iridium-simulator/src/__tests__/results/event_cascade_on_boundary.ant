## start=0 end=20 points=200 atol=1e-12 rtol=1e-6

// More of a fake cascade. The event should run once, then
// CVODE should try to take a step and detect a root.

C' = 2
C = 1

at C > 3: C = 5
at C > 5: C = 10
