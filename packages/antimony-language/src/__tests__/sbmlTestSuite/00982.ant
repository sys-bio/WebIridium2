## start=0.0 end=2.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00982()

  // Assignment Rules:
  temp := time/2;
  y := delay(x, temp);

  // Rate Rules:
  x' = 1;

  // Variable initializations:
  x = 0;

  // Other declarations:
  var x, temp, y;
end

case00982 is "case00982"
