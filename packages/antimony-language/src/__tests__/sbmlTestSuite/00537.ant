## start=0.0 end=50.0 points=50 atol=0.001 rtol=0.0001 selections=X0,X1,T amounts=X0,X1,T
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00537()

  // Compartments and Species:
  compartment C;
  species X0 in C, X1 in C, T in C;

  // Algebraic Rules:
  _alg0: 0 = k2 + -0.2;

  // Reactions:
  reaction1: X0 => T; C*k1*X0;
  reaction2: T => X1; C*k2*T;

  // Species initializations:
  X0 = 1.25/C;
  X0 has substance_per_volume;
  X1 = 1.5/C;
  X1 has substance_per_volume;
  T = 1/C;
  T has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.1;
  k2 = ;

  // Other declarations:
  var k2;
  const C, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00537 is "case00537"
