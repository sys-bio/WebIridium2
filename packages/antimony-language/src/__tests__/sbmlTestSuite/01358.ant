## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x amounts=
## tags=CSymbolTime,EventWithDelay,Parameter,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,NonConstantParameter,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Events:
  E0: at J0 after time > 0.5: x = 2;

  // Variable initializations:
  J0 = 2;
  x = 1;

  // Other declarations:
  var x;
  const J0;
end

sub1 is "sub1"

model *case01358()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.J0 is J1;

  // Variable initializations:
  J1 = 5;
end

case01358 is "case01358"
