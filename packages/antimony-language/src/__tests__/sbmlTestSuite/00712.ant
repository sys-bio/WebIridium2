## start=0.0 end=5.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,FunctionDefinition,RateRule,Amount,NonConstantParameter,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00712()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Rate Rules:
  k1' = k2 + k3;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 0.15/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 2.5;
  C has volume;

  // Variable initializations:
  k1 = 1;
  k2 = 0.2;
  k3 = 0.3;

  // Other declarations:
  var k1;
  const C, k2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00712 is "case00712"
