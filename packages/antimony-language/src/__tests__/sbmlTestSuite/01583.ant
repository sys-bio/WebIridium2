## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=k1,S1,C amounts=S1
## tags=Compartment,EventNoDelay,EventPriority,Parameter,RateRule,Reaction,Species,Amount,AssignedVariableStoichiometry,NonConstantParameter,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01583()

  // Compartments and Species:
  compartment C;
  species S1 in C;

  // Rate Rules:
  k1' = 1;

  // Reactions:
  J0:  -> S1_stoich S1; 0.1;

  // Events:
  E0: at k1 > 4.5, priority = 10: S1_stoich = 2;
  E1: at k1 > 4.5, priority = 5: S1_stoich = 3;

  // Species initializations:
  S1 = 1;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  k1 = 0;
  S1_stoich = 1;

  // Other declarations:
  var k1, S1_stoich;
  const C;
end

case01583 is "case01583"
