## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,S2,C,parameterId_0 amounts=S1,S2
## tags=Compartment,Parameter,RateRule,Reaction,Species,StoichiometryMath,Amount,AssignedConstantStoichiometry,AssignedVariableStoichiometry,DefaultValue,NonConstantParameter,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01634()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Assignment Rules:
  S1_degrade := 3;
  S2_create := parameterId_0;

  // Rate Rules:
  parameterId_0' = 1;

  // Reactions:
  J0: S1_degrade S1 -> S2_create S2; 1;

  // Species initializations:
  S1 = 30/C;
  S2 = 0;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  parameterId_0 = 1;

  // Other declarations:
  var parameterId_0;
  const C;
end

case01634 is "case01634"
