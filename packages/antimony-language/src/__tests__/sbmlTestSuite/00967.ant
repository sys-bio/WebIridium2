## start=0.0 end=10.0 points=101 atol=0.01 rtol=0.01 selections=Q,R amounts=
## tags=CSymbolTime,EventNoDelay,EventPriority,Parameter,NonConstantParameter,EventIsNotPersistent

// Created by libAntimony v3.0.0
model *case00967()

  // Events:
  Qinc: at (time - reset) >= 0.01, priority = time, persistent=false: Q = Q + 0.01, reset = time;
  Rinc: at (time - reset) >= 0.01, priority = 5.001, persistent=false: R = R + 0.01, reset = time;

  // Variable initializations:
  reset = 0;
  Q = 0;
  R = 0;

  // Other declarations:
  var reset, Q, R;
end

case00967 is "case00967"
