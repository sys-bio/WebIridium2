## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x amounts=
## tags=NonConstantParameter,NonUnityCompartment,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Compartments and Species:
  compartment J0;

  // Events:
  E0: at time > (J0 + 0.5): x = 4;

  // Compartment initializations:
  J0 = 2;

  // Variable initializations:
  x = 1;

  // Other declarations:
  var x;
  const J0;
end

sub1 is "sub1"

model *case01364()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.J0 is J1;

  // Compartment initializations:
  J1 = 5;
end

case01364 is "case01364"
