## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=DelayInTrigger,EventIsNotPersistent,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01519()

  // Rate Rules:
  P1' = 1;

  // Events:
  E0: at 1 after (delay(P1, 1) > 1.5) && (delay(P1, 1) < 2), persistent=false: P2 = 3;

  // Variable initializations:
  P1 = 0;
  P2 = 0;

  // Other declarations:
  var P1, P2;
end

case01519 is "case01519"
