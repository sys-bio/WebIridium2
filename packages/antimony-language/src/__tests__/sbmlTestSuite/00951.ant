## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=P,Q,R amounts=
## tags=Parameter,testType:      TimeCourse

// Created by libAntimony v3.0.0
model *case000951()

  // Variable initializations:
  P = inf;
  Q = -inf;
  R = NaN;

  // Other declarations:
  const P, Q, R;

  // Unit definitions:
  unit volume = litre;
  unit time_unit = time_unit;

  // Notes:
  model notes "Originally created by libAntimony v1.4 (using libSBML 5.0.0-a1)"
end
