## start=0.0 end=2.0 points=40 atol=0.0001 rtol=0.0001 selections=x,y amounts=x
## tags=Amount,NonConstantParameter,InitialValueReassigned,UncommonMathML

// Created by libAntimony v3.0.0
model *case00938()

  // Compartments and Species:
  species x;

  // Assignment Rules:
  y := 2 + delay(x, 0.2);

  // Reactions:
  _J0:  => x; 1;

  // Species initializations:
  x = 1*sin(10*time);

  // Other declarations:
  var y;

  // Notes:
  model notes "Originally created by libAntimony 1.4 (using libSBML 5.0.0-a1)"
end

case00938 is "case00938"
