## start=0.0 end=25.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01044()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Algebraic Rules:
  _alg0: 0 = kf + -0.75;

  // Reactions:
  reaction1: S3 -> S1 + S2; -(C*(kf*S1*S2 + -1*kr*S3));

  // Species initializations:
  S1 = 0.01/C;
  S1 has substance_per_volume;
  S2 = 0.02/C;
  S2 has substance_per_volume;
  S3 = 0.01/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  kf = ;
  kr = 0.25;

  // Other declarations:
  var kf;
  const C, kr;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case01044 is "case01044"
