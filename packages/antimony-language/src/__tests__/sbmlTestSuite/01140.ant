## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=s8 amounts=s8
## tags=Amount,HasOnlySubstanceUnits,MultiCompartment,comp:ConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  substanceOnly species $s80 in C;

  // Rate Rules:
  s80' = 4*s80 + 3;

  // Species initializations:
  s80 = 80/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

model *case01140()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.s80 * conv is s8;

  // Compartments and Species:
  compartment C;

  // Species initializations:
  s8 = 8/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  conv = 0.1;

  // Other declarations:
  const C, conv;
end

case01140 is "case01140"
