## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=x amounts=
## tags=CSymbolTime,EventWithDelay,Parameter,NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case01213()

  // Events:
  E0: at piecewise(0.5, 1 <= 2 <= 1, 0.3) after time > 0.01: x = 7;

  // Variable initializations:
  x = 3;

  // Other declarations:
  var x;
end

case01213 is "case01213"
