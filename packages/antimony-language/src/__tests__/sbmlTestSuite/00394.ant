## start=0.0 end=1.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00394()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Assignment Rules:
  S1_S2_stoichiometry := 4*p1;

  // Reactions:
  reaction1: S1 + S1_S2_stoichiometry S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Events:
  event1: at S1 < 0.75: S1 = 1, S2 = 1.75;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 2/C;
  S2 has substance_per_volume;
  S3 = 1/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;
  p1 = 0.5;

  // Other declarations:
  const C, k1, k2, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;
end

case00394 is "case00394"
