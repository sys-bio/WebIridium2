## start=0.0 end=2.0 points=50 atol=1e-08 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount,LocalParameters,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00612()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, $S4 in C;

  // Assignment Rules:
  S4 := k*S2;

  // Reactions:
  reaction1: S1 + S2 => S3; C*reaction1_k*S1*S2;
  reaction2: S3 => S1 + S2; C*reaction2_k*S3;

  // Species initializations:
  S1 = 1e-05/C;
  S1 has substance_per_volume;
  S2 = 1.5e-05/C;
  S2 has substance_per_volume;
  S3 = 1e-05/C;
  S3 has substance_per_volume;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k = 1.5;
  reaction1_k = 15000;
  reaction2_k = 5;

  // Other declarations:
  const C, k;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00612 is "case00612"
