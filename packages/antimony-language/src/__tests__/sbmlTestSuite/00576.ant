## start=0.0 end=10.0 points=50 atol=1e-06 rtol=0.0001 selections=p1,p2,p3,p4 amounts=
## tags=Parameter,RateRule,AlgebraicRule,NonConstantParameter

// Created by libAntimony v3.0.0
model *case00576()

  // Rate Rules:
  p1' = k2*p3 + -1*k1*p1*p2;
  p3' = k1*p1*p2 + -1*k2*p3;
  p2' = k2*p3 + -1*k1*p1*p2;

  // Algebraic Rules:
  _alg0: 0 = p4 + -1*p1;

  // Variable initializations:
  p1 = 0.001;
  k2 = 0.25;
  p3 = 0.001;
  k1 = 0.75;
  p2 = 0.002;
  p4 = 0.001;

  // Other declarations:
  var p1, p3, p2, p4;
  const k2, k1;

  // Unit definitions:
  unit time_unit = time_unit;
end

case00576 is "case00576"
