## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=spec,Spec,sPeC,C amounts=spec,Spec,sPeC
## tags=Compartment,Species,Amount

// Created by libAntimony v3.0.0
model *case01804()

  // Compartments and Species:
  compartment C;
  species spec in C, Spec in C, sPeC in C;

  // Species initializations:
  spec = 3;
  Spec = 4;
  sPeC = 5;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end
