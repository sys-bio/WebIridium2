## start=0.0 end=15.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00538()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Algebraic Rules:
  _alg0: 0 = k2 + -0.25;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.0001/C;
  S1 has substance_per_volume;
  S2 = 0.0002/C;
  S2 has substance_per_volume;
  S3 = 0.0001/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = ;

  // Other declarations:
  var k2;
  const C, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00538 is "case00538"
