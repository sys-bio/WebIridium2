## start=0.0 end=10.0 points=50 atol=1e-09 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,FunctionDefinition,Amount,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00282()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: S1 + S2 => S3; compartment_*(k1*S1*S2);
  reaction2: S3 => S1 + S2; compartment_*k2*S3;

  // Species initializations:
  S1 = 1e-06/compartment_;
  S1 has substance_per_area;
  S2 = 2e-06/compartment_;
  S2 has substance_per_area;
  S3 = 1e-06/compartment_;
  S3 has substance_per_area;

  // Compartment initializations:
  compartment_ = 6.8;
  compartment_ has area;

  // Variable initializations:
  k1 = 3.24;
  k2 = 0.125;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case00282 is "case00282"
