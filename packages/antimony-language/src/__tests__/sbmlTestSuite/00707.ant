## start=0.0 end=2.5 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,EventNoDelay,Amount,LocalParameters

// Created by libAntimony v3.0.0
model *case00707()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C;

  // Assignment Rules:
  S3 := k*S2;

  // Reactions:
  reaction1: S1 => S2; C*reaction1_k*S1;

  // Events:
  event1: at S1 < 0.25: S2 = 1;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 0.5/C;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k = 0.75;
  reaction1_k = 5;

  // Other declarations:
  const C, k;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00707 is "case00707"
