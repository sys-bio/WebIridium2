## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,sub1__S1,sub2__S1,sub2__sub1__S1,sub3__S1,sub3__sub1__S1,sub3__sub2__S1 amounts=
## tags=Concentration,ConstantSpecies,MultiCompartment,NonUnityCompartment,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  species $S1 in C;

  // Species initializations:
  S1 = 7/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

model moddef2()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();

  // Compartments and Species:
  compartment C;
  species $S1 in C;

  // Species initializations:
  S1 = 9/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

model moddef3()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub2: moddef2();

  // Compartments and Species:
  compartment C;
  species $S1 in C;

  // Species initializations:
  S1 = 11/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

model *case01132()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub2: moddef2();
  sub3: moddef3();
  sub2.sub1.C is C;
  sub3.sub2.sub1.S1 is S1;

  // Species initializations:
  S1 = 5/C;

  // Compartment initializations:
  C = 10;
end

case01132 is "case01132"
