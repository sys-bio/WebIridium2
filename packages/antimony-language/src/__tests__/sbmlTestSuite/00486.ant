## start=0.0 end=2.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00486()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_length;
  S2 = 2/C;
  S2 has substance_per_length;
  S3 = 1/C;
  S3 has substance_per_length;

  // Compartment initializations:
  C = 2*p1;
  C has length;

  // Variable initializations:
  p1 = 0.5;
  k1 = 3.5;
  k2 = 1.5;

  // Other declarations:
  const C, p1, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit length = metre;
  unit time_unit = time_unit;
  unit substance_per_length = mole / metre;
end

case00486 is "case00486"
