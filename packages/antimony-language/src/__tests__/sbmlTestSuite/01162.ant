## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=p8 amounts=
## tags=Parameter,comp:Deletion,comp:ModelDefinition,comp:Port,comp:ReplacedElement,comp:Submodel,testType:        TimeCourse

// Created by libAntimony v3.0.0

// Warnings from automatic translation:
//    Unable to create port rr_port in model moddef1 because RateRule elements only exist as part of other Antimony elements, and do not function as their own separate entities which may be flagged as a port.

model moddef1()

  // Rate Rules:
  p80' = 4*p80 + 3;

  // Variable initializations:
  p80 = 80;

  // Other declarations:
  var p80;
end

model *case01162()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.p80 is p8;

  // Variable initializations:
  p8 = 8;
end

case01162 is "case01162"
