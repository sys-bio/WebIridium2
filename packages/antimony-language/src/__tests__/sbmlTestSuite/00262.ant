## start=0.0 end=20.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount,NonUnityStoichiometry,0D-Compartment

// Created by libAntimony v3.0.0
model *case00262()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species S1 in compartment_, S2 in compartment_;

  // Reactions:
  reaction1: S1 => 2 S2; k1*S1;
  reaction2: 2 S2 => S1; k2*S2^2;

  // Species initializations:
  S1 = 0.00015/compartment_;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  compartment_ = ;

  // Variable initializations:
  k1 = 0.35;
  k2 = 180;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00262 is "case00262"
