## start=0.0 end=1.0 points=50 atol=0.01 rtol=0.0001 selections=A1,A2,A3,A4 amounts=A1,A2,A3,A4
## tags=Amount,ReversibleReaction,FastReaction

// Created by libAntimony v3.0.0
model *case00870()

  // Compartments and Species:
  compartment C;
  species A1 in C, A2 in C, A3 in C, A4 in C;

  // Reactions:
  fasterReaction: A4 -> A2; (Kf1*A4 + -(Kr1*A2))*C;
  slowerReaction1: A1 + A2 -> A3; (Kf*A1*A2 + -(Kr*A3))*C;

  // Species initializations:
  A1 = 2/C;
  A1 has substance_per_volume;
  A2 = 3/C;
  A2 has substance_per_volume;
  A3 = 4/C;
  A3 has substance_per_volume;
  A4 = 1/C;
  A4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  Kr1 = 2;
  Kf1 = 1;
  Kf = 1;
  Kr = 2;

  // Other declarations:
  const C, Kr1, Kf1, Kf, Kr;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00870 is "case00870"
