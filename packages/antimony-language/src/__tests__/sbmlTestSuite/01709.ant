## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,S1,C amounts=S1
## tags=CSymbolTime,Compartment,EventWithDelay,Parameter,RateRule,Species,Amount,EventUsesAssignmentTimeValues,HasOnlySubstanceUnits,NonConstantParameter,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case01709()

  // Compartments and Species:
  compartment C;
  substanceOnly species $S1 in C;

  // Rate Rules:
  S1' = -1;

  // Events:
  E1: at 2 after time >= 2.5, fromTrigger=false: P1 = S1 + 1;

  // Species initializations:
  S1 = 10/C;

  // Compartment initializations:
  C = 2;

  // Variable initializations:
  P1 = 0;

  // Other declarations:
  var P1;
  const C;
end

case01709 is "case01709"
