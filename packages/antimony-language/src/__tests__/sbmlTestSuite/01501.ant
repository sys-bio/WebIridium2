## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,S2,C,P0,P1,sconversion,mconversion amounts=S1,S2
## tags=AlgebraicRule,Compartment,Parameter,Reaction,Species,Amount,ConversionFactors,InitialValueReassigned,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01501()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Algebraic Rules:
  _alg0: 0 = P0 - S1;
  _alg1: 0 = P1 - S2;

  // Reactions:
  J0: S1 -> S2; 0.1*S1;

  // Species initializations:
  S1 = 5;
  S2 = 1;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  P0 = ;
  P1 = ;
  mconversion = 3;
  sconversion = 2;

  // Other declarations:
  var P0, P1;
  const C, mconversion, sconversion;
end

case01501 is "case01501"
