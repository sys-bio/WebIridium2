## start=0.0 end=25.0 points=50 atol=0.001 rtol=0.0001 selections=X0,X1,T amounts=X0,X1,T
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00536()

  // Compartments and Species:
  compartment C;
  species X0 in C, X1 in C, T in C;

  // Algebraic Rules:
  _alg0: 0 = k1 + -0.1;

  // Reactions:
  reaction1: X0 => T; C*k1*X0;
  reaction2: T => X1; C*k2*T;

  // Species initializations:
  X0 = 1/C;
  X0 has substance_per_volume;
  X1 = 0;
  X1 has substance_per_volume;
  T = 0;
  T has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = ;
  k2 = 0.375;

  // Other declarations:
  var k1;
  const C, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00536 is "case00536"
