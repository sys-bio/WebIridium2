## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,S2,S3,S4,m_cf,s1_cf,C amounts=S1,S2,S3,S4
## tags=Amount,AssignedVariableStoichiometry,BoundaryCondition,ConversionFactors,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01736()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C, $S4 in C;

  // Rate Rules:
  S3_stoich' = 0.01;
  S1_stoich' = 0.01;
  S2_stoich' = 0.01;
  S4_stoich' = 0.01;

  // Reactions:
  J0: S3_stoich $S3 + S1_stoich S1 -> S2_stoich S2 + S4_stoich $S4; 0.01;

  // Species initializations:
  S1 = 2/C;
  S2 = 3/C;
  S3 = 2/C;
  S4 = 3/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  m_cf = 3;
  s1_cf = 5;
  S3_stoich = 1;
  S1_stoich = 1;
  S2_stoich = 1;
  S4_stoich = 1;

  // Other declarations:
  var S3_stoich, S1_stoich, S2_stoich, S4_stoich;
  const C, m_cf, s1_cf;
end

case01736 is "case01736"
