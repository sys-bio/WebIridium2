## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2,P3,P4 amounts=
## tags=AssignmentRule,CSymbolTime,InitialAssignment,Parameter,InitialValueReassigned,NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case01343()

  // Assignment Rules:
  P2 := tanh(-0.4);
  P3 := tanh(time);
  P4 := tanh(-time);

  // Variable initializations:
  P1 = tanh(0.3);

  // Other declarations:
  var P2, P3, P4;
  const P1;
end

case01343 is "case01343"
