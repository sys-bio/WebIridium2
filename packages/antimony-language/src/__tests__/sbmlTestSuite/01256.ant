## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,p2,c amounts=S1
## tags=AssignmentRule,CSymbolRateOf,Compartment,Parameter,Reaction,Species,Amount,InitialValueReassigned,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01256()

  // Compartments and Species:
  compartment c;
  species S1 in c;

  // Assignment Rules:
  p2 := rateOf(S1);

  // Reactions:
  J0:  => S1; 0.01*S1;

  // Species initializations:
  S1 = 1/c;

  // Compartment initializations:
  c = 1;

  // Other declarations:
  var p2;
  const c;
end

case01256 is "case01256"
