## start=0.0 end=1.0 points=50 atol=0.01 rtol=0.0001 selections=c amounts=
## tags=NonConstantCompartment,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00907()

  // Compartments and Species:
  compartment c;

  // Rate Rules:
  c' = -1*(c/k1);

  // Compartment initializations:
  c = 22.5;
  c has length;

  // Variable initializations:
  k1 = 0.175;

  // Other declarations:
  var c;
  const k1;

  // Unit definitions:
  unit length = metre;
  unit time_unit = time_unit;
end

case00907 is "case00907"
