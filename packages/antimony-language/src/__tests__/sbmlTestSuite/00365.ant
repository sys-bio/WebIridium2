## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,0D-Compartment

// Created by libAntimony v3.0.0
model *case00365()

  // Compartments and Species:
  compartment C;
  substanceOnly species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; k1*S1*S2;
  reaction2: S3 => S1 + S2; k2*S3;

  // Events:
  event1: at S1 < 0.7: S2 = 1;
  event2: at S3 > 1.2: S1 = 0.95;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 2/C;
  S2 has substance_per_volume;
  S3 = 1/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = ;

  // Variable initializations:
  k1 = 0.075;
  k2 = 0.025;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00365 is "case00365"
