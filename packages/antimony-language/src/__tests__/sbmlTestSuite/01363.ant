## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x amounts=
## tags=Compartment,InitialAssignment,Parameter,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,InitialValueReassigned,NonUnityCompartment,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Compartments and Species:
  compartment J0;

  // Compartment initializations:
  J0 = 2;

  // Variable initializations:
  x = J0 + 2;

  // Other declarations:
  const J0, x;
end

sub1 is "sub1"

model *case01363()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.J0 is J1;

  // Compartment initializations:
  J1 = 5;
end

case01363 is "case01363"
