## start=0.0 end=2.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=
## tags=NonConstantParameter

// Created by libAntimony v3.0.0
model *case00173()

  // Rate Rules:
  S1' = -1*factorial(ceil(p1*S1))*p2^-1;
  S2' = factorial(ceil(p1*S1))*p2^-1;

  // Variable initializations:
  S1 = 1;
  p1 = 4;
  p2 = 25;
  S2 = 0;

  // Other declarations:
  var S1, S2;
  const p1, p2;

  // Unit definitions:
  unit time_unit = time_unit;
end

case00173 is "case00173"
