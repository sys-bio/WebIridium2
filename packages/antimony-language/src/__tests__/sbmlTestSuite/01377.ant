## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x,A_C amounts=J1
## tags=AlgebraicRule,Compartment,Parameter,Species,comp:ModelDefinition,comp:ReplacedBy,comp:ReplacedElement,comp:Submodel,Amount,BoundaryCondition,ConstantSpecies,InitialValueReassigned,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Compartments and Species:
  compartment C;
  species $J0 in C;

  // Algebraic Rules:
  _alg0: 0 = x - J0;

  // Species initializations:
  J0 = 2/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  x = ;

  // Other declarations:
  var x;
  const C;
end

sub1 is "sub1"

model *case01377()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.C is A_C;
  A.J0 is J1;

  // Algebraic Rules:
  A._alg0: 0 = A.x - J1;

  // Species initializations:
  J1 = 5;
end

case01377 is "case01377"
