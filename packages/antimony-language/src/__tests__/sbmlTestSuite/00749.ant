## start=0.0 end=2.5 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Concentration,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00749()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C;

  // Assignment Rules:
  S3 := k1*S2;

  // Reactions:
  reaction1: S1 => S2; C*k2*S1;

  // Events:
  event1: at S1 < 0.25: S2 = 1;

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 0.5;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 0.79;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 5;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00749 is "case00749"
