## start=0.0 end=5.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3,S4 amounts=
## tags=Concentration,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00738()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, $S4 in C;

  // Assignment Rules:
  S4 := p1*S2;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.01;
  S1 has substance_per_volume;
  S2 = 0.02;
  S2 has substance_per_volume;
  S3 = 0.015;
  S3 has substance_per_volume;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 0.86;
  C has volume;

  // Variable initializations:
  p1 = 0.75;
  k1 = 75;
  k2 = 0.025;

  // Other declarations:
  const C, p1, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00738 is "case00738"
