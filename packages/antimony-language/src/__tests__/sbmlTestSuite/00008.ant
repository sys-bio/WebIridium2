## start=0.0 end=10.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00008()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, $S2 in compartment_;

  // Reactions:
  reaction1: S1 => $S2; compartment_*k1*S1;
  reaction2: $S2 => S1; compartment_*k2*S2;

  // Species initializations:
  S1 = 0.15/compartment_;
  S1 has substance_per_volume;
  S2 = 0.15/compartment_;
  S2 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 0.5;
  k2 = 0.25;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00008 is "case00008"
