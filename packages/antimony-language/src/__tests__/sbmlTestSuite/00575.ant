## start=0.0 end=10.0 points=50 atol=1e-07 rtol=0.0001 selections=p1,p2,p3,p4 amounts=
## tags=Parameter,RateRule,AlgebraicRule,NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00575()

  // Rate Rules:
  p1' = -1*k1*p1*p2 + k2*p3 + k3*p3;
  p2' = k2*p3 + -1*k1*p1*p2;
  p3' = k1*p1*p2 + -1*k2*p3 + -1*k3*p3;
  p4' = k3*p3;

  // Algebraic Rules:
  _alg0: 0 = k2 + -0.9;

  // Variable initializations:
  p1 = 0.0002;
  k1 = 10000;
  p2 = 0.0002;
  k2 = ;
  p3 = 0;
  k3 = 0.7;
  p4 = 0;

  // Other declarations:
  var p1, p2, k2, p3, p4;
  const k1, k3;

  // Unit definitions:
  unit time_unit = time_unit;
end

case00575 is "case00575"
