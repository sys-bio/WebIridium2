## start=0.0 end=3.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,EventWithDelay,Concentration,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00690()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C;

  // Assignment Rules:
  S3 := k1*S2;

  // Reactions:
  reaction1: S1 => S2; C*k2*S1;

  // Events:
  event1: at 1 after S1 < 0.25: S2 = 0.75;

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 2.5;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 2.5;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00690 is "case00690"
