## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=
## tags=Concentration,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00792()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 1.5;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = k2*100^-1;
  k2 = 50;

  // Other declarations:
  var k1;
  const C, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00792 is "case00792"
