## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=s1,C,extentconv amounts=s1
## tags=Amount,HasOnlySubstanceUnits,comp:ExtentConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  substanceOnly species s1 in C;

  // Reactions:
  J0:  -> s1; 10;

  // Species initializations:
  s1 = 1/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

moddef1 is "moddef1"

model moddef2()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
end

moddef2 is "moddef2"

model *case01470()

  // Sub-modules, and any changes to those submodules:
  sub2: moddef2(), extentconv = extentconv;
  sub2.sub1.C is C;
  sub2.sub1.s1 is s1;

  // Compartments and Species:
  species s1 in C;

  // Variable initializations:
  extentconv = 1000;

  // Other declarations:
  const extentconv;
end

case01470 is "case01470"
