## start=0.0 end=3.0 points=50 atol=0.01 rtol=0.0001 selections=A1,A2,A3,A4 amounts=A1,A2,A3,A4
## tags=Compartment,Species,Reaction,Parameter,Amount,FastReaction,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00874()

  // Compartments and Species:
  compartment C;
  species A1 in C, A2 in C, $A3 in C, A4 in C;

  // Reactions:
  reaction1: A4 => A2; A4*C*Kf1;
  reaction2: A1 + A2 => $A3; A1*A2*C*Kf;

  // Species initializations:
  A1 = 2/C;
  A1 has substance_per_volume;
  A2 = 3/C;
  A2 has substance_per_volume;
  A3 = 4/C;
  A3 has substance_per_volume;
  A4 = 1/C;
  A4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  Kf1 = 1;
  Kf = 1;

  // Other declarations:
  const C, Kf1, Kf;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00874 is "case00874"
