## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=sub1__s1 amounts=sub1__s1
## tags=Compartment,Parameter,Reaction,Species,comp:ModelDefinition,comp:Submodel,Amount,HasOnlySubstanceUnits,comp:ExtentConversionFactor,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  substanceOnly species s1 in C;

  // Reactions:
  J0:  -> s1; 10;

  // Species initializations:
  s1 = 1/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

model *case01143()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1(), extentconv = extentconv;

  // Variable initializations:
  extentconv = 1000;

  // Other declarations:
  const extentconv;
end

case01143 is "case01143"
