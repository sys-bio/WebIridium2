## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=AssignmentRule,CSymbolTime,EventWithDelay,Parameter,EventUsesTriggerTimeValues,InitialValueReassigned,NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case01597()

  // Assignment Rules:
  P1 := cosh(time);

  // Events:
  E0: at 1 after time > 4.5: P2 = cosh(time);

  // Variable initializations:
  P2 = 1;

  // Other declarations:
  var P2, P1;
end

case01597 is "case01597"
