## start=0.0 end=8.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,InitialAssignment,Concentration,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00788()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Assignment Rules:
  k1 := 0.75;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 0.133333333333333*k1;
  S1 has substance_per_volume;
  S2 = 0.15;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 6.6;
  C has volume;

  // Other declarations:
  var k1;
  const C;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00788 is "case00788"
