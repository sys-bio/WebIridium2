## start=0.0 end=2.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00984()

  // Assignment Rules:
  y := delay(x, temp);

  // Rate Rules:
  x' = 1;

  // Events:
  _E0: at time >= 0.99: temp = 1;

  // Variable initializations:
  temp = 0;
  x = 0;

  // Other declarations:
  var temp, y, x;
end

case00984 is "case00984"
