## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=p1,p2,p3 amounts=
## tags=InitialValueReassigned,L3v2MathML,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01281()

  // Assignment Rules:
  p1 := min(-5, -time);
  p2 := min(time);
  p3 := min(5, time, 7);

  // Other declarations:
  var p1, p2, p3;
end

case01281 is "case01281"
