## start=0.0 end=10.0 points=100 atol=0.0001 rtol=0.0001 selections=X amounts=X
## tags=Compartment,EventNoDelay,Parameter,Reaction,Species,Amount,AssignedVariableStoichiometry,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00972()

  // Compartments and Species:
  species X;

  // Reactions:
  J0:  -> Xref X; k1;

  // Events:
  _E0: at X >= 5: Xref = 3;

  // Species initializations:
  X = 0;

  // Variable initializations:
  Xref = 1;
  k1 = 1;

  // Other declarations:
  var Xref;
  const k1;
end

case00972 is "case00972"
