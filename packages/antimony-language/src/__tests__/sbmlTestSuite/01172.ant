## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=t1 amounts=
## tags=CSymbolTime,Parameter,RateRule,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,NonConstantParameter,comp:TimeConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Rate Rules:
  t1' = time/t1 + 3;

  // Variable initializations:
  t1 = 1;

  // Other declarations:
  var t1;
end

model *case01172()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1(), timeconv = timeconv;
  sub1.t1 is t1;

  // Variable initializations:
  timeconv = 60;

  // Other declarations:
  const timeconv;
end

case01172 is "case01172"
