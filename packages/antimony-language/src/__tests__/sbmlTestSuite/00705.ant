## start=0.0 end=25.0 points=50 atol=0.001 rtol=0.0001 selections=X0,X1,T,S1 amounts=X0,X1,T,S1
## tags=Compartment,Species,Reaction,Parameter,AlgebraicRule,AssignmentRule,Amount,LocalParameters,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00705()

  // Compartments and Species:
  compartment C;
  species X0 in C, X1 in C, T in C, S1 in C;

  // Assignment Rules:
  k := 0.1;

  // Algebraic Rules:
  _alg0: 0 = (k3 + 1)*S1 + -1*T;

  // Reactions:
  reaction1: X0 => T; C*k*X0;
  reaction2: T => X1; C*reaction2_k*S1;

  // Species initializations:
  X0 = 1/C;
  X0 has substance_per_volume;
  X1 = 0;
  X1 has substance_per_volume;
  T = 0;
  T has substance_per_volume;
  S1 = 0;
  S1 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k3 = 2.5;
  reaction2_k = 0.375;

  // Other declarations:
  var k;
  const C, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00705 is "case00705"
