## start=0.0 end=1.0 points=2 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e amounts=
## tags=InitialValueReassigned

// Created by libAntimony v3.0.0
model *case_01490()

  // Assignment Rules:
  a := plus(1);
  b := times(2);
  c := piecewise(1, and(x), 0);
  d := piecewise(1, or(x), 0);
  e := piecewise(1, xor(x), 0);

  // Variable initializations:
  x = ;

  // Other declarations:
  var a, b, c, d, e;
end

case_01490 is "case_01490"
