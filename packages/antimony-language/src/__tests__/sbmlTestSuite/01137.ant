## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=p8 amounts=
## tags=AssignmentRule,Parameter,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,InitialValueReassigned,comp:ConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Assignment Rules:
  p80 := 80;

  // Other declarations:
  var p80;
end

model *case01137()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.p80 * conv is p8;

  // Variable initializations:
  conv = 0.1;

  // Other declarations:
  const conv;
end

case01137 is "case01137"
