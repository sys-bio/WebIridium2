## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=param1 amounts=
## tags=testType:        TimeCourse

// Created by libAntimony v3.0.0
model Mod1(subparam1)

  // Variable initializations:
  subparam1 = 5.01;

  // Other declarations:
  const subparam1;
end

model *case01125()

  // Sub-modules, and any changes to those submodules:
  submod1: Mod1(param1);

  // Variable initializations:
  param1 = 10.42;
end

case01125 is "case01125"
