## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=x amounts=
## tags=NonConstantParameter

// Created by libAntimony v3.0.0
model *case01215()

  // Rate Rules:
  x' = piecewise(1, 2 > 1 > 2, 0);

  // Variable initializations:
  x = 3;

  // Other declarations:
  var x;
end

case01215 is "case01215"
