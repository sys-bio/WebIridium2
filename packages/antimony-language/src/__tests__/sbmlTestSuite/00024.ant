## start=0.0 end=25.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount,ConstantSpecies,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00024()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, $S3 in compartment_, S4 in compartment_;

  // Reactions:
  reaction1: S1 => S2; compartment_*k1*S1;
  reaction2: S2 => S1; compartment_*k2*S2;
  reaction3: S2 => $S3 + S4; compartment_*k3*S2;
  reaction4: $S3 + S4 => S2; compartment_*k4*S3*S4;

  // Species initializations:
  S1 = 0.0001/compartment_;
  S1 has substance_per_volume;
  S2 = 0.0002/compartment_;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;
  S4 = 0;
  S4 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;
  k3 = 0.4;
  k4 = 0.1;

  // Other declarations:
  const compartment_, k1, k2, k3, k4;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00024 is "case00024"
