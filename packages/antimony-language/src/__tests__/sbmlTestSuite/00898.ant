## start=0.0 end=6.0 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00898()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Assignment Rules:
  S1_S2_stoichiometry := 2*p1;

  // Reactions:
  reaction1: S1 => S1_S2_stoichiometry S2; C*k1*S1*time;

  // Species initializations:
  S1 = 0.0015/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 1;
  p1 = 1;

  // Other declarations:
  const C, k1, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;
end

case00898 is "case00898"
