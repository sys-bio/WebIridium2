## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount,LocalParameters

// Created by libAntimony v3.0.0
model *case00830()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Reactions:
  reaction1: S1 -> S2; C*(reaction1_kf*S1 + -1*reaction1_kr*S2);
  reaction2: S3 -> S4; C*(kf*S3 + -1*kr*S4);

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 1.5/C;
  S3 has substance_per_volume;
  S4 = 0.5/C;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  kf = 0.9;
  kr = 0.075;
  reaction1_kf = 0.8;
  reaction1_kr = 0.06;

  // Other declarations:
  const C, kf, kr;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00830 is "case00830"
