## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,c,p1 amounts=S1
## tags=Amount,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01259()

  // Compartments and Species:
  compartment c;
  species S1 in c;

  // Rate Rules:
  p1' = 1;

  // Reactions:
  J0:  => S1; rateOf(p1);

  // Species initializations:
  S1 = 10/c;

  // Compartment initializations:
  c = 1;

  // Variable initializations:
  p1 = 2;

  // Other declarations:
  var p1;
  const c;
end

case01259 is "case01259"
