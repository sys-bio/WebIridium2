## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=EventIsNotPersistent,InitialValueReassigned,NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case01595()

  // Assignment Rules:
  P1 := cosh(time);

  // Events:
  E0: at 1 after (cosh(time) > 9) && (cosh(time) < 11), persistent=false: P2 = 3;

  // Variable initializations:
  P2 = 1;

  // Other declarations:
  var P2, P1;
end

case01595 is "case01595"
