## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,S2,C,P1 amounts=S1,S2
## tags=Compartment,InitialAssignment,Parameter,Reaction,Species,Amount,FastReaction,InitialValueReassigned,NonUnityStoichiometry,SpeciesReferenceInMath

// Created by libAntimony v3.0.0
model *case01558()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  J0: S1 -> S2_stoich S2; S1*0.1;

  // Species initializations:
  S1 = 2/C;
  S2 = 3/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  P1 = S2_stoich;
  S2_stoich = 2;

  // Other declarations:
  const C, P1;
end

case01558 is "case01558"
