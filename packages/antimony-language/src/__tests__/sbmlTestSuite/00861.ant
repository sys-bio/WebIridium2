## start=0.0 end=6.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2,S3,S4 amounts=
## tags=Concentration,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00861()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1*time;
  reaction2: S2 => S3; C*k2*S2*time;
  reaction3: S3 => S4; C*k3*S3*time;

  // Species initializations:
  S1 = 0.1;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;
  S4 = 0;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1.78;
  C has volume;

  // Variable initializations:
  k1 = 0.7;
  k2 = 0.5;
  k3 = 1;

  // Other declarations:
  const C, k1, k2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00861 is "case00861"
