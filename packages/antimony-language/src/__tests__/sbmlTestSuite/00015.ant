## start=0.0 end=2.5 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount

// Created by libAntimony v3.0.0
model *case00015()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, S3 in compartment_, S4 in compartment_;

  // Reactions:
  reaction1: S1 + S2 => S3 + S4; compartment_*k1*S1*S2;
  reaction2: S3 + S4 => S1 + S2; compartment_*k2*S3*S4;

  // Species initializations:
  S1 = 0.001/compartment_;
  S1 has substance_per_volume;
  S2 = 0.001/compartment_;
  S2 has substance_per_volume;
  S3 = 0.002/compartment_;
  S3 has substance_per_volume;
  S4 = 0.001/compartment_;
  S4 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 750;
  k2 = 250;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00015 is "case00015"
