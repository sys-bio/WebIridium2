## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=p8,sub1__prel amounts=
## tags=InitialValueReassigned,NonConstantParameter,comp:ConversionFactor,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model moddef1()

  // Assignment Rules:
  prel := p80/8;

  // Variable initializations:
  p80 = 80;

  // Other declarations:
  var p80, prel;
end

model *case01138()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.p80 * conv is p8;

  // Variable initializations:
  p8 = 8;
  conv = 0.1;

  // Other declarations:
  const conv;
end

case01138 is "case01138"
