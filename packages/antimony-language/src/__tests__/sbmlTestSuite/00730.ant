## start=0.0 end=5.0 points=50 atol=0.01 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=StoichiometryMath,Compartment,Species,Reaction,Parameter,RateRule,EventNoDelay,Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case00730()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C, $S4 in C;

  // Assignment Rules:
  reaction1_S1_stoichiometry := 4*p1;

  // Rate Rules:
  S3' = 0.5*k1;
  S4' = -0.5*k2;

  // Reactions:
  reaction1: reaction1_S1_stoichiometry S1 => S2; C*k1*S1;

  // Events:
  event1: at S1 < 0.5: S2 = 2;

  // Species initializations:
  S1 = 1.5/C;
  S1 has substance_per_volume;
  S2 = 2/C;
  S2 has substance_per_volume;
  S3 = 1.5/C;
  S3 has substance_per_volume;
  S4 = 4/C;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;
  p1 = 0.25;

  // Other declarations:
  const C, k1, k2, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;
end

case00730 is "case00730"
