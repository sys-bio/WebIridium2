## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=p1,p2,p3 amounts=
## tags=InitialAssignment,Parameter,InitialValueReassigned,L3v2MathML

// Created by libAntimony v3.0.0
model *case01276()

  // Variable initializations:
  p1 = min(-1, -10);
  p2 = min(30);
  p3 = min(20, 200, 2, 2000);

  // Other declarations:
  const p1, p2, p3;
end

case01276 is "case01276"
