## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=s1,C,timeconv amounts=s1
## tags=Amount,comp:TimeConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  substanceOnly species s1 in C;

  // Reactions:
  J0:  -> s1; 10;

  // Species initializations:
  s1 = 1/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

moddef1 is "moddef1"

model *moddef2()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
end

moddef2 is "moddef2"
