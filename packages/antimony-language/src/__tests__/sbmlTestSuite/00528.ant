## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,InitialAssignment,FunctionDefinition,Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00528()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 1.5/C;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k1 = k2/100;
  k2 = 50;

  // Other declarations:
  var k1;
  const C, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00528 is "case00528"
