## start=0.0 end=3.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount,NonConstantCompartment,NonUnityCompartment,NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00318()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Assignment Rules:
  C := p1*p2;

  // Rate Rules:
  p2' = 0.1;

  // Reactions:
  reaction1: S1 + S2 => S3 + S4; C*k1*S1*S2;
  reaction2: S3 + S4 => S1 + S2; C*k2*S3*S4;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 1.5/C;
  S2 has substance_per_volume;
  S3 = 1.1/C;
  S3 has substance_per_volume;
  S4 = 1/C;
  S4 has substance_per_volume;
  C has volume;

  // Variable initializations:
  p1 = 0.1;
  p2 = 0.9;
  k1 = 0.0365;
  k2 = 0.0025;

  // Other declarations:
  var C, p2;
  const p1, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00318 is "case00318"
