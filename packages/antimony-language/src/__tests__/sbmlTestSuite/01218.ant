## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e,f amounts=
## tags=AssignmentRule,InitialAssignment,Parameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01218()

  // Assignment Rules:
  b := a + 1;
  d := c + 1;
  f := e + 1;

  // Variable initializations:
  a = 4/2;
  c = b + 1;
  e = d + 1;

  // Other declarations:
  var b, d, f;
  const a, c, e;
end

case01218 is "case01218"
