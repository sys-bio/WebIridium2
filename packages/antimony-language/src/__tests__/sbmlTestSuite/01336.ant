## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=EventNoDelay,EventPriority,Parameter,EventIsNotPersistent,EventT0Firing,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01336()

  // Events:
  E1: at x > 2, priority = 10, t0=false: x = 0;
  E2: at x > 2, priority = 5, t0=false, persistent=false: y = 7;

  // Variable initializations:
  x = 3;
  y = 6;

  // Other declarations:
  var x, y;
end

case01336 is "case01336"
