## start=0.0 end=0.1 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,InitialAssignment,Amount,NonUnityCompartment,LocalParameters,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00027()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_;

  // Reactions:
  reaction1: S1 => S2; compartment_*reaction1_k*S1;

  // Species initializations:
  S1 = 0.015/compartment_;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 0.534;
  compartment_ has volume;

  // Variable initializations:
  reaction1_k = 100;

  // Other declarations:
  const compartment_;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00027 is "case00027"
