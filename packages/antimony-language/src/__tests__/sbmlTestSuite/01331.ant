## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=CSymbolTime,EventNoDelay,EventPriority,Parameter,EventUsesAssignmentTimeValues,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01331()

  // Events:
  E1: at time > 3.5, priority = 10: x = 7;
  E2: at time > 3.5, priority = 5, fromTrigger=false: y = x;

  // Variable initializations:
  x = 3;
  y = 0;

  // Other declarations:
  var x, y;
end

case01331 is "case01331"
