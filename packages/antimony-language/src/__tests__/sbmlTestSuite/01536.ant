## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=S1,P1,C amounts=S1
## tags=CSymbolDelay,CSymbolTime,Compartment,EventNoDelay,Parameter,RateRule,Reaction,Species,Amount,AssignedVariableStoichiometry,DelayInEventAssignment,NonConstantParameter,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01536()

  // Compartments and Species:
  compartment C;
  species S1 in C;

  // Rate Rules:
  P1' = 1;

  // Reactions:
  J0:  -> S1_stoich S1; 1;

  // Events:
  _E0: at time > 3: S1_stoich = delay(P1, 1);

  // Species initializations:
  S1 = 2/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  S1_stoich = 1;
  P1 = 0;

  // Other declarations:
  var S1_stoich, P1;
  const C;
end

case01536 is "case01536"
