## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=EventUsesTriggerTimeValues,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01330()

  // Events:
  E1: at time > 3.5, priority = 10: x = 7;
  E2: at time > 3.5, priority = 5: y = x;

  // Variable initializations:
  x = 3;
  y = 0;

  // Other declarations:
  var x, y;
end

case01330 is "case01330"
