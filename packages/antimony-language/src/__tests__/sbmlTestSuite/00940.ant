## start=0.0 end=1.0 points=50 atol=0.0001 rtol=0.0001 selections=x,y amounts=x
## tags=Amount,NonConstantParameter,InitialValueReassigned,UncommonMathML

// Created by libAntimony v3.0.0
model *case00940()

  // Compartments and Species:
  species x;

  // Assignment Rules:
  y := 2 + delay(x, 0.5);

  // Species initializations:
  x = 1*sin(10*time);
  x has substance_per_volume;

  // Other declarations:
  var y;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Notes:
  model notes "Originally created by libAntimony 1.4 (using libSBML 5.0.0-a1)"
end

case00940 is "case00940"
