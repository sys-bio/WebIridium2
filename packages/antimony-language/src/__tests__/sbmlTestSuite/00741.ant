## start=0.0 end=5.0 points=50 atol=1e-09 rtol=0.0001 selections=S1,S2,S3,S4 amounts=
## tags=Concentration,NonConstantParameter,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00741()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C, S4 in C;

  // Rate Rules:
  k1' = p1*(k2 + k3);

  // Reactions:
  reaction1: S1 + S2 => S3 + S4; C*k1*S1*S2;
  reaction2: S3 + S4 => S1 + S2; C*k2*S3*S4;

  // Species initializations:
  S1 = 1e-06;
  S1 has substance_per_volume;
  S2 = 1.5e-06;
  S2 has substance_per_volume;
  S3 = 2e-06;
  S3 has substance_per_volume;
  S4 = 5e-07;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 0.15;
  C has volume;

  // Variable initializations:
  k1 = 1000000;
  p1 = 1;
  k2 = 300000;
  k3 = 700000;

  // Other declarations:
  var k1;
  const C, p1, k2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00741 is "case00741"
