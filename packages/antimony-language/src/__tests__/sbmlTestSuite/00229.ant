## start=0.0 end=0.4 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00229()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, $S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: S1 + $S2 => S3; compartment_*k1*S1*S2;
  reaction2: S3 => S1 + $S2; compartment_*k2*S3;

  // Species initializations:
  S1 = 1/compartment_;
  S1 has substance_per_length;
  S2 = 2/compartment_;
  S2 has substance_per_length;
  S3 = 1.5/compartment_;
  S3 has substance_per_length;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has length;

  // Variable initializations:
  k1 = 7.5;
  k2 = 0.3;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit length = metre;
  unit time_unit = time_unit;
  unit substance_per_length = mole / metre;

  // Display Names:
  compartment_ is "compartment";
end

case00229 is "case00229"
