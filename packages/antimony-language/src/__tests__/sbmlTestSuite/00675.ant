## start=0.0 end=5.0 points=50 atol=0.001 rtol=0.0001 selections=X0,X1,T,S1 amounts=X0,X1,T,S1
## tags=Amount,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00675()

  // Compartments and Species:
  compartment C;
  species X0 in C, X1 in C, T in C, S1 in C;

  // Assignment Rules:
  k1 := 0.1;

  // Algebraic Rules:
  _alg0: 0 = (k3 + 1)*S1 + -1*T;

  // Reactions:
  reaction1: X0 => T; C*k1*X0;
  reaction2: T => X1; C*k2*S1;

  // Species initializations:
  X0 = 1/C;
  X0 has substance_per_volume;
  X1 = 0;
  X1 has substance_per_volume;
  T = 0;
  T has substance_per_volume;
  S1 = 0;
  S1 has substance_per_volume;

  // Compartment initializations:
  C = 9.8;
  C has volume;

  // Variable initializations:
  k2 = 0.375;
  k3 = 2.5;

  // Other declarations:
  var k1;
  const C, k2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00675 is "case00675"
