## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x amounts=
## tags=CSymbolTime,Compartment,EventNoDelay,Parameter,comp:ModelDefinition,comp:ReplacedElement,comp:Submodel,NonConstantParameter,NonUnityCompartment,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Compartments and Species:
  compartment J0;

  // Events:
  E0: at time > 5.5: x = J0;

  // Compartment initializations:
  J0 = 2;

  // Variable initializations:
  x = 1;

  // Other declarations:
  var x;
  const J0;
end

sub1 is "sub1"

model *case01366()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.J0 is J1;

  // Compartment initializations:
  J1 = 5;
end

case01366 is "case01366"
