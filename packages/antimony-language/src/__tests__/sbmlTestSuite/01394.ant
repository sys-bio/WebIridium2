## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e,f,g amounts=
## tags=AssignmentRule,InitialAssignment,Parameter,comp:ModelDefinition,comp:Port,comp:ReplacedElement,comp:Submodel,InitialValueReassigned,NonConstantParameter

// Created by libAntimony v3.0.0
model sub1(a, b, c, d, e, f, g)

  // Assignment Rules:
  b := a;
  f := e;

  // Variable initializations:
  a = 1;
  c = 3;
  d = c;
  e = 4;
  g = 5;

  // Other declarations:
  var b, d, f;
  const a, c, e, g;
end

sub1 is "sub1"

model *case01394()

  // Sub-modules, and any changes to those submodules:
  A: sub1(a, b, c, d, e, f, g);

  // Assignment Rules:
  e := d;

  // Variable initializations:
  c = b;
  g = f;

  // Other declarations:
  var c, e, g;
end

case01394 is "case01394"
