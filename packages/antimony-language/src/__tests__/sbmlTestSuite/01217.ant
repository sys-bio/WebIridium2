## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e,f amounts=
## tags=InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01217()

  // Assignment Rules:
  b := c + 1;
  d := e + 1;
  f := 4/2;

  // Variable initializations:
  a = b + 1;
  c = d + 1;
  e = f + 1;

  // Other declarations:
  var b, d, f;
  const a, c, e;
end

case01217 is "case01217"
