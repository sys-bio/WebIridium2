## start=0.0 end=1.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Compartment,Species,Reaction,Parameter,Amount,0D-Compartment,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00238()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species $S1 in compartment_, S2 in compartment_;

  // Reactions:
  reaction1: $S1 => S2; k1*S1;

  // Species initializations:
  S1 = 0.15/compartment_;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  compartment_ = ;

  // Variable initializations:
  k1 = 1.023;

  // Other declarations:
  const compartment_, k1;

  // Unit definitions:
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00238 is "case00238"
