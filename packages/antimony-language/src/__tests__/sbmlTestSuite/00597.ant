## start=0.0 end=5.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Concentration,LocalParameters

// Created by libAntimony v3.0.0
model *case00597()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 => S2; C*k*S1;
  reaction2: S2 => S3; C*reaction2_S1*S2;

  // Species initializations:
  S1 = 0.03;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k = 2;
  reaction2_S1 = 1;

  // Other declarations:
  const C, k;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00597 is "case00597"
