## start=0.0 end=8.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Compartment,Species,Reaction,Parameter,Concentration,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00816()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 -> S3; C*(kf*S1*S2 + -1*kr*S3);

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 0.5;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 2.3;
  C has volume;

  // Variable initializations:
  kf = 1.1;
  kr = 0.09;

  // Other declarations:
  const C, kf, kr;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00816 is "case00816"
