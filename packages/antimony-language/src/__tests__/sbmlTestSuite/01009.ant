## start=0.0 end=0.25 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,Amount,HasOnlySubstanceUnits,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case01009()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: S1 + S2 => S3; k1*S1*S2;
  reaction2: S3 => S1 + S2; k2*S3;

  // Species initializations:
  S1 = 0.1/compartment_;
  S1 has substance_per_area;
  S2 = 0.2/compartment_;
  S2 has substance_per_area;
  S3 = 0.1/compartment_;
  S3 has substance_per_area;

  // Compartment initializations:
  compartment_ = 10;
  compartment_ has area;

  // Variable initializations:
  k1 = 75;
  k2 = 25;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case01009 is "case01009"
