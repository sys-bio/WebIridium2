## start=0.0 end=2.0 points=10 atol=0.0002 rtol=0.0001 selections=x,y amounts=
## tags=NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00983()

  // Assignment Rules:
  y := delay(x, temp);

  // Rate Rules:
  x' = 1;

  // Algebraic Rules:
  _alg0: 0 = -1*temp + time/2;

  // Variable initializations:
  x = 0;
  temp = ;

  // Other declarations:
  var x, temp, y;
end

case00983 is "case00983"
