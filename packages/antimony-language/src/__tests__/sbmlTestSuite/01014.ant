## start=0.0 end=5.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,HasOnlySubstanceUnits,InitialValueReassigned,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case01014()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species S1 in compartment_, S2 in compartment_, $S3 in compartment_;

  // Assignment Rules:
  S3 := k1*S2;

  // Reactions:
  reaction1: S1 => S2; k2*S1;

  // Species initializations:
  S1 = 0.1/compartment_;
  S1 has substance_per_volume;
  S2 = 0.15/compartment_;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 10;
  compartment_ has volume;

  // Variable initializations:
  k1 = 1.05;
  k2 = 1.15;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case01014 is "case01014"
