## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=C8 amounts=
## tags=NonConstantCompartment,NonUnityCompartment,comp:ConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C80;

  // Rate Rules:
  C80' = 4*C80 + 3;

  // Compartment initializations:
  C80 = 80;

  // Other declarations:
  var C80;
end

model *case01141()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1();
  sub1.C80 * conv is C8;

  // Compartment initializations:
  C8 = 8;

  // Variable initializations:
  conv = 0.1;

  // Other declarations:
  const conv;
end

case01141 is "case01141"
