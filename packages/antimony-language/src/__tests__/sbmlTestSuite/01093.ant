## start=0.0 end=10.0 points=50 atol=0.01 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=InitialValueReassigned,Amount,AssignedConstantStoichiometry,NonUnityStoichiometry,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case01093()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, $S3 in C, $S4 in C;

  // Rate Rules:
  S3' = k2*S4;
  S4' = -1*k2*S4;

  // Reactions:
  reaction1: S1 => generatedId_0 S2; C*k1*S1;

  // Species initializations:
  S1 = 2*p1;
  S1 has substance_per_volume;
  S2 = 2/C;
  S2 has substance_per_volume;
  S3 = 1.5/C;
  S3 has substance_per_volume;
  S4 = 4/C;
  S4 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = 0.75;
  k2 = 0.5;
  k1 = 0.75;
  p2 = 0.25;
  generatedId_0 = 4*p2;

  // Other declarations:
  const C, p1, k2, k1, p2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case01093 is "case01093"
