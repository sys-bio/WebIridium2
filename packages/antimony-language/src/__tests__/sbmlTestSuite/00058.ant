## start=0.0 end=5.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,Amount,LocalParameters

// Created by libAntimony v3.0.0
model *case00058()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: S1 => S2; compartment_*k*S1;
  reaction2: S2 => S3; compartment_*reaction2_k*S2;

  // Species initializations:
  S1 = 0.0003/compartment_;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k = 1;
  reaction2_k = 2;

  // Other declarations:
  const compartment_, k;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00058 is "case00058"
