## start=0.0 end=20.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Amount,AssignedConstantStoichiometry,DefaultValue,NonUnityStoichiometry,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00519()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Assignment Rules:
  S2_S3_stoichiometry := 4*p1;

  // Reactions:
  reaction1: S1 + S2 => S2_S3_stoichiometry S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 2*p1;
  S1 has substance_per_volume;
  S2 = 0.02/C;
  S2 has substance_per_volume;
  S3 = 0.01/C;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  p1 = 0.00125;
  k1 = 0.75;
  k2 = 0.25;
  p2 = 0.5;

  // Other declarations:
  const C, p1, k1, k2, p2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit substance_per_volume = mole / litre;
end

case00519 is "case00519"
