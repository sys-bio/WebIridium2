## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=CSymbolRateOf,CSymbolTime,EventWithDelay,Parameter,RateRule,EventIsNotPersistent,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01526()

  // Rate Rules:
  P1' = time;

  // Events:
  E0: at 2 after (rateOf(P1) > 1.5) && (rateOf(P1) < 2.5), persistent=false: P2 = 3;

  // Variable initializations:
  P1 = 0;
  P2 = 0;

  // Other declarations:
  var P1, P2;
end

case01526 is "case01526"
