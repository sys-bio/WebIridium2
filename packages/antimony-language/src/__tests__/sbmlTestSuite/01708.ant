## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,S1,C amounts=S1
## tags=Amount,EventUsesTriggerTimeValues,HasOnlySubstanceUnits,NonConstantParameter,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case01708()

  // Compartments and Species:
  compartment C;
  substanceOnly species $S1 in C;

  // Rate Rules:
  S1' = -1;

  // Events:
  E1: at 2 after time >= 2.5: P1 = S1 + 1;

  // Species initializations:
  S1 = 10/C;

  // Compartment initializations:
  C = 2;

  // Variable initializations:
  P1 = 0;

  // Other declarations:
  var P1;
  const C;
end

case01708 is "case01708"
