## start=0.0 end=5.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,FunctionDefinition,RateRule,Amount,NonConstantParameter

// Created by libAntimony v3.0.0
model *case00124()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Rate Rules:
  k1' = p1*(k2*p2 + k3);

  // Reactions:
  reaction1: S1 + S2 => S3; compartment_*k1*S1*S2;
  reaction2: S3 => S1 + S2; compartment_*k2*S3;

  // Species initializations:
  S1 = 1/compartment_;
  S1 has substance_per_volume;
  S2 = 2/compartment_;
  S2 has substance_per_volume;
  S3 = 1/compartment_;
  S3 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 1.7;
  p1 = 1;
  k2 = 0.3;
  p2 = 1;
  k3 = -0.1;

  // Other declarations:
  var k1;
  const compartment_, p1, k2, p2, k3;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00124 is "case00124"
