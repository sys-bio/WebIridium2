## start=0.0 end=5.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2 amounts=
## tags=Compartment,Species,Reaction,Parameter,,Concentration

// Created by libAntimony v3.0.0
model *case00589()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 0.015;
  S1 has substance_per_area;
  S2 = 0;
  S2 has substance_per_area;

  // Compartment initializations:
  C = 1;
  C has area;

  // Variable initializations:
  k1 = 1;

  // Other declarations:
  const C, k1;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;
end

case00589 is "case00589"
