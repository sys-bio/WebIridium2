## start=0.0 end=15.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Concentration

// Created by libAntimony v3.0.0
model *case00593()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.01;
  S1 has substance_per_length;
  S2 = 0.02;
  S2 has substance_per_length;
  S3 = 0.01;
  S3 has substance_per_length;

  // Compartment initializations:
  C = 1;
  C has length;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit length = metre;
  unit time_unit = time_unit;
  unit substance_per_length = mole / metre;
end

case00593 is "case00593"
