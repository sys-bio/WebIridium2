## start=0.0 end=6.0 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3,S4 amounts=S1,S2,S3,S4
## tags=Amount,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00346()

  // Compartments and Species:
  compartment compartment_;
  species $S1 in compartment_, S2 in compartment_, $S3 in compartment_, $S4 in compartment_;

  // Rate Rules:
  S3' = 0.0005*k1;
  S4' = -0.0005*k2;

  // Reactions:
  reaction1: $S1 => S2; compartment_*k1*S1;

  // Species initializations:
  S1 = 0.0015/compartment_;
  S1 has substance_per_volume;
  S2 = 0.002/compartment_;
  S2 has substance_per_volume;
  S3 = 0.0015/compartment_;
  S3 has substance_per_volume;
  S4 = 0.004/compartment_;
  S4 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Variable initializations:
  k1 = 0.693;
  k2 = 0.25;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00346 is "case00346"
