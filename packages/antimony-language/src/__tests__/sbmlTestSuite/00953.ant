## start=0.0 end=100.0 points=1001 atol=0.01 rtol=0.01 selections=S amounts=
## tags=NonConstantParameter,EventIsNotPersistent,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00953()

  // Assignment Rules:
  S := Q + R;

  // Events:
  Qinc: at (time - reset) >= 0.01, persistent=false: Q = Q + 0.01, reset = time;
  Rinc: at (time - reset) >= 0.01, persistent=false: R = R + 0.01, reset = time;

  // Variable initializations:
  reset = 0;
  Q = 0;
  R = 0;
  error = 0;

  // Other declarations:
  var reset, Q, R, S, error;

  // Unit definitions:
  unit volume = litre;
  unit time_unit = time_unit;

  // Notes:
  model notes "Originally created by libAntimony v1.4 (using libSBML 5.0.0-a1)"
end

case00953 is "case00953"
