## start=0.0 end=3.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=StoichiometryMath,Compartment,Species,Reaction,Parameter,Amount,DefaultValue,ReversibleReaction,AssignedConstantStoichiometry,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01028()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Assignment Rules:
  generatedId_0 := 4*p1;

  // Reactions:
  reaction1: S3 -> S1 + generatedId_0 S2; -(C*(kf*S1*S2 + -1*kr*S3));

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 0.5/C;
  S2 has substance_per_volume;
  S3 = 0;
  S3 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  kf = 1.1;
  kr = 0.09;
  p1 = 0.5;

  // Other declarations:
  const C, kf, kr, p1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = second;
  unit substance_per_volume = mole / litre;
end

case01028 is "case01028"
