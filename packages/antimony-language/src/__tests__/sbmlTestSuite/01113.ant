## start=0.0 end=1.0 points=2 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e amounts=
## tags=InitialValueReassigned,UncommonMathML

// Created by libAntimony v3.0.0
model *case_01113()

  // Variable initializations:
  a = plus(1);
  b = times(2);
  c = piecewise(1, and(true), 0);
  d = piecewise(1, or(true), 0);
  e = piecewise(1, xor(true), 0);

  // Other declarations:
  const a, b, c, d, e;
end

case_01113 is "case_01113"
