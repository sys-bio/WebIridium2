## start=0.0 end=1.0 points=50 atol=0.0001 rtol=0.0001 selections=x,y amounts=
## tags=NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case00941()

  // Assignment Rules:
  y := 2 + delay(x, 0.5);

  // Variable initializations:
  x = sin(10*time);

  // Other declarations:
  var y, x;

  // Notes:
  model notes "Originally created by libAntimony 1.4 (using libSBML 5.0.0-a1)"
end

case00941 is "case00941"
