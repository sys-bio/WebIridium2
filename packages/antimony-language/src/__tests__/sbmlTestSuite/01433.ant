## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=A,C amounts=A
## tags=Compartment,Reaction,Species,Amount,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01433()

  // Compartments and Species:
  compartment C;
  species A in C;

  // Reactions:
  J0: A + 2 A + -4 A -> 5 A + -2 A + A + -1 A; 1;

  // Species initializations:
  A = 1;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

case01433 is "case01433"
