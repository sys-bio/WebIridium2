## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=J1,A__x,A_C amounts=J1
## tags=Amount,BoundaryCondition,InitialValueReassigned,comp:SubmodelOutput

// Created by libAntimony v3.0.0
model sub1()

  // Compartments and Species:
  compartment C;
  species $J0 in C;

  // Species initializations:
  J0 = 2/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  x = J0 + 2;

  // Other declarations:
  const C, x;
end

sub1 is "sub1"

model *case01372()

  // Sub-modules, and any changes to those submodules:
  A: sub1();
  A.C is A_C;
  A.J0 is J1;

  // Species initializations:
  J1 = 5;
end

case01372 is "case01372"
