## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=A,B,C amounts=A,B
## tags=Amount,NonUnityStoichiometry

// Created by libAntimony v3.0.0
model *case01432()

  // Compartments and Species:
  compartment C;
  species A in C, B in C;

  // Reactions:
  J0: 2 A + -1 B -> -1 A + 2 B; 1;

  // Species initializations:
  A = 30;
  B = 1;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  const C;
end

case01432 is "case01432"
