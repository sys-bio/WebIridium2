## start=0.0 end=3.0 points=50 atol=0.001 rtol=0.0001 selections=s,c amounts=
## tags=Concentration,NonConstantCompartment,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00926()

  // Compartments and Species:
  compartment c;
  species s in c;

  // Rate Rules:
  c' = 0.5*c;

  // Species initializations:
  s = 2;
  s has substance_per_volume;

  // Compartment initializations:
  c = 1;
  c has volume;

  // Other declarations:
  var c;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00926 is "case00926"
