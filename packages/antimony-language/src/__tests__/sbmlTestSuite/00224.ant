## start=0.0 end=1.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,Amount,HasOnlySubstanceUnits

// Created by libAntimony v3.0.0
model *case00224()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species S1 in compartment_, S2 in compartment_, S3 in compartment_;

  // Reactions:
  reaction1: S1 + S2 => S3; k1*S1*S2;
  reaction2: S3 => S1 + S2; k2*S3;

  // Species initializations:
  S1 = 1/compartment_;
  S1 has substance_per_length;
  S2 = 2/compartment_;
  S2 has substance_per_length;
  S3 = 1.5/compartment_;
  S3 has substance_per_length;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has length;

  // Variable initializations:
  k1 = 7.5;
  k2 = 0.3;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit length = metre;
  unit time_unit = time_unit;
  unit substance_per_length = mole / metre;

  // Display Names:
  compartment_ is "compartment";
end

case00224 is "case00224"
