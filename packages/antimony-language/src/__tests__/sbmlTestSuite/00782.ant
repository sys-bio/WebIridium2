## start=0.0 end=8.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=
## tags=Compartment,Species,Reaction,Parameter,InitialAssignment,Concentration,NonUnityCompartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00782()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*k1*S1;

  // Species initializations:
  S1 = 1;
  S1 has substance_per_volume;
  S2 = 1.5;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = k2*10^-1;
  C has volume;

  // Variable initializations:
  k2 = 50;
  k1 = 0.5;

  // Other declarations:
  var C;
  const k2, k1;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00782 is "case00782"
