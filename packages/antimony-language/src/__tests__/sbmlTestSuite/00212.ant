## start=0.0 end=0.75 points=50 atol=1e-06 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount,BoundaryCondition

// Created by libAntimony v3.0.0
model *case00212()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, $S2 in compartment_;

  // Reactions:
  reaction1: S1 => $S2; compartment_*k1*S1;

  // Species initializations:
  S1 = 0.0015/compartment_;
  S1 has substance_per_area;
  S2 = 0.001/compartment_;
  S2 has substance_per_area;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has area;

  // Variable initializations:
  k1 = 1;

  // Other declarations:
  const compartment_, k1;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case00212 is "case00212"
