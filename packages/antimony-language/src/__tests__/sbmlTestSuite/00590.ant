## start=0.0 end=20.0 points=50 atol=1e-07 rtol=0.0001 selections=S1,S2,S3 amounts=
## tags=Compartment,Species,Reaction,Parameter,Concentration

// Created by libAntimony v3.0.0
model *case00590()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C, S3 in C;

  // Reactions:
  reaction1: S1 + S2 => S3; C*k1*S1*S2;
  reaction2: S3 => S1 + S2; C*k2*S3;

  // Species initializations:
  S1 = 0.0001;
  S1 has substance_per_area;
  S2 = 0.0002;
  S2 has substance_per_area;
  S3 = 0.0001;
  S3 has substance_per_area;

  // Compartment initializations:
  C = 1;
  C has area;

  // Variable initializations:
  k1 = 0.75;
  k2 = 0.25;

  // Other declarations:
  const C, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;
end

case00590 is "case00590"
