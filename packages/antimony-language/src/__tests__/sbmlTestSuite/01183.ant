## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=s1,sub1__p80 amounts=s1
## tags=Amount,HasOnlySubstanceUnits,InitialValueReassigned,MultiCompartment,comp:ConversionFactor,comp:ExtentConversionFactor,comp:SubmodelOutput,comp:TimeConversionFactor

// Created by libAntimony v3.0.0
model moddef1()

  // Compartments and Species:
  compartment C;
  substanceOnly species s1 in C;

  // Assignment Rules:
  p80 := J0 + 6;

  // Reactions:
  J0:  -> s1; 10;

  // Species initializations:
  s1 = 1/C;

  // Compartment initializations:
  C = 1;

  // Other declarations:
  var p80;
  const C;
end

model *case01183()

  // Sub-modules, and any changes to those submodules:
  sub1: moddef1(), timeconv = timeconv, extentconv = extentconv;
  sub1.J0 * extentpertimeconv is J0;

  // Compartments and Species:
  compartment C;
  substanceOnly species s1 in C;

  // Reactions:
  J0:  -> s1; 10;

  // Species initializations:
  s1 = 1/C;

  // Compartment initializations:
  C = 1;

  // Variable initializations:
  timeconv = 60;
  extentconv = 1000;
  extentpertimeconv = 16.6666666666667;

  // Other declarations:
  const timeconv, extentconv, C, extentpertimeconv;
end

case01183 is "case01183"
