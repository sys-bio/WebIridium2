## start=0.0 end=2.0 points=10 atol=0.0001 rtol=0.0001 selections=x,y,z amounts=
## tags=NonConstantParameter,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00985()

  // Assignment Rules:
  z := delay(x, 1);
  y := delay(x, z);

  // Rate Rules:
  x' = 1;

  // Variable initializations:
  x = 0;

  // Other declarations:
  var z, x, y;
end

case00985 is "case00985"
