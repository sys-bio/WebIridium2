## start=0.0 end=1.0 points=50 atol=0.0001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount,NonUnityCompartment

// Created by libAntimony v3.0.0
model *case00280()

  // Compartments and Species:
  compartment compartment_;
  species S1 in compartment_, S2 in compartment_;

  // Reactions:
  reaction1: S1 => S2; compartment_*(k1*S1);

  // Species initializations:
  S1 = 0.15/compartment_;
  S1 has substance_per_area;
  S2 = 0.05/compartment_;
  S2 has substance_per_area;

  // Compartment initializations:
  compartment_ = 3.5;
  compartment_ has area;

  // Variable initializations:
  k1 = 1.75;

  // Other declarations:
  const compartment_, k1;

  // Unit definitions:
  unit substance = mole;
  unit area = metre^2;
  unit time_unit = time_unit;
  unit substance_per_area = mole / metre^2;

  // Display Names:
  compartment_ is "compartment";
end

case00280 is "case00280"
