## start=0.0 end=10.0 points=50 atol=0.001 rtol=0.0001 selections=S1,S2 amounts=S1,S2
## tags=Amount

// Created by libAntimony v3.0.0
model *case00803()

  // Compartments and Species:
  compartment C;
  species S1 in C, S2 in C;

  // Reactions:
  reaction1: S1 => S2; C*S1*k^-1;

  // Species initializations:
  S1 = 1/C;
  S1 has substance_per_volume;
  S2 = 0;
  S2 has substance_per_volume;

  // Compartment initializations:
  C = 1;
  C has volume;

  // Variable initializations:
  k = 1.5;

  // Other declarations:
  const C, k;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;
end

case00803 is "case00803"
