## start=0.0 end=1.0 points=10 atol=0.0001 rtol=0.0001 selections=x,z amounts=
## tags=InitialValueReassigned,NonConstantParameter,UncommonMathML

// Created by libAntimony v3.0.0
model *case01199()

  // Assignment Rules:
  z := piecewise(1, piecewise(true, x >= 0.49, false), 0);

  // Rate Rules:
  x' = 1;

  // Variable initializations:
  x = 0;

  // Other declarations:
  var x, z;
end

case01199 is "case01199"
