## start=0.0 end=1.0 points=2 atol=0.0001 rtol=0.0001 selections=a,b amounts=
## tags=InitialValueReassigned,UncommonMathML

// Created by libAntimony v3.0.0
model *case_01116()

  // Variable initializations:
  a = piecewise(1, true);
  b = piecewise(1, a > 0, 2, a <= 0);

  // Other declarations:
  const a, b;
end

case_01116 is "case_01116"
