## start=0.0 end=1.0 points=2 atol=0.0001 rtol=0.0001 selections=a,b,c,d,e amounts=
## tags=InitialValueReassigned

// Created by libAntimony v3.0.0
model *case_01491()

  // Assignment Rules:
  a := 1 + (1 + plus());
  b := 2*(2*times());
  c := piecewise(1, (and() && y) && y, 0);
  d := piecewise(1, (or() || y) || y, 0);
  e := piecewise(1, xor(xor(xor(), y), y), 0);

  // Variable initializations:
  y = ;

  // Other declarations:
  var a, b, c, d, e;
end

case_01491 is "case_01491"
