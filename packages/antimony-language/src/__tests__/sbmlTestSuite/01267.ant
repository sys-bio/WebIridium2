## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=p1,p2 amounts=
## tags=NonConstantParameter

// Created by libAntimony v3.0.0
model *case01267()

  // Rate Rules:
  p1' = 0.01*p1;

  // Events:
  E0: at time > 5.5, priority = 0.021: p2 = 5;
  E1: at time > 5.5, priority = rateOf(p1): p2 = 3;

  // Variable initializations:
  p2 = 10;
  p1 = 2;

  // Other declarations:
  var p2, p1;
end

case01267 is "case01267"
