## start=0.0 end=0.2 points=50 atol=1e-05 rtol=0.0001 selections=S1,S2,S3 amounts=S1,S2,S3
## tags=Compartment,Species,Reaction,Parameter,AssignmentRule,Amount,0D-Compartment,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00156()

  // Compartments and Species:
  compartment compartment_;
  substanceOnly species S1 in compartment_, S2 in compartment_, $S3 in compartment_;

  // Assignment Rules:
  S3 := k1*S2;

  // Reactions:
  reaction1: S1 => S2; k2*S1;

  // Species initializations:
  S1 = 0.01/compartment_;
  S1 has substance_per_volume;
  S2 = 0.015/compartment_;
  S2 has substance_per_volume;
  S3 has substance_per_volume;

  // Compartment initializations:
  compartment_ = ;

  // Variable initializations:
  k1 = 0.75;
  k2 = 50;

  // Other declarations:
  const compartment_, k1, k2;

  // Unit definitions:
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00156 is "case00156"
