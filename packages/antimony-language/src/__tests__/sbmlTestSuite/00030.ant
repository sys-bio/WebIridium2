## start=0.0 end=2.0 points=50 atol=0.0001 rtol=0.0001 selections=S1 amounts=S1
## tags=Amount,InitialValueReassigned

// Created by libAntimony v3.0.0
model *case00030()

  // Compartments and Species:
  compartment compartment_;
  species $S1 in compartment_;

  // Assignment Rules:
  S1 := 7;
  S1 has substance_per_volume;

  // Compartment initializations:
  compartment_ = 1;
  compartment_ has volume;

  // Other declarations:
  const compartment_;

  // Unit definitions:
  unit volume = litre;
  unit substance = mole;
  unit time_unit = time_unit;
  unit substance_per_volume = mole / litre;

  // Display Names:
  compartment_ is "compartment";
end

case00030 is "case00030"
