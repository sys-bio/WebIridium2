## start=0.0 end=10.0 points=10 atol=0.0001 rtol=0.0001 selections=P1,P2 amounts=
## tags=CSymbolDelay,EventWithDelay,Parameter,RateRule,DelayInEventAssignment,EventUsesAssignmentTimeValues,NonConstantParameter

// Created by libAntimony v3.0.0
model *case01524()

  // Rate Rules:
  P1' = 1;

  // Events:
  E0: at 2 after P1 > 1.5, fromTrigger=false: P2 = delay(P1, 1);

  // Variable initializations:
  P1 = 0;
  P2 = 0;

  // Other declarations:
  var P1, P2;
end

case01524 is "case01524"
